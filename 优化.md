# 国际物流全流程跟踪系统 · 优化方案

> 版本：v1 | 日期：2026-09-08 | 状态：待评审
> 背景：对现行 Demo（PRD v6.2，巴西模板 + 青岛港子配置）做一次**业务能力补强**设计。目标不是加功能堆料，而是把系统从「单证打勾的时间轴」升级为「由货物 + 动态驱动的可重排时间轴」。

---

## 0. 方案总览

| 项 | 说明 |
| :-- | :-- |
| **定位演进** | 「什么时间办什么单证」→「这批货是什么、船现在到哪、接下来怎么排」 |
| **联网决定** | **实时 AIS / 船司船期抓取一律不做**（纯本地离线）。船舶动态完全依赖操作员人工标注 |
| **两大 P0** | D1 货物·班轮维度；D2 时间·风险维度（全环节推迟/提前 `shift_node`） |
| **核心机制** | 静态计划 与 实际动态 分离 → 任一节点可「推迟 / 提前」→ 驱动下游与 ETA 联动重排 → 单证 due 与甘特自动刷新 |

> 一句话：**动态来源只有人工，所以「推迟/提前」是唯一时间调整手段，必须做得又全又可靠。**

---

## 1. 现状问题诊断

当前系统对「**流程/单证**」覆盖出色（12 节点 + 泳道 + 单证清单提醒），但对「**货物、船舶、时间动态**」三个物流本体对象是空白，导致：

1. **不知道货是什么**：时间轴/甘特只表达"什么时间办什么单"，不承载每件设备的尺寸、重量、超限参数。
2. **不知道船到哪**：船名/航次/IMO 只能塞进节点备注，无专门模型，位置动态靠人工翻系统。
3. **时间轴是静态的**：`generate_schedule` 只在建项目时跑一次，中途"船晚到 3 天"不会联动境外段。
4. **推迟不完整**：现有 `delay_nodes` 只支持推迟（正天数）、不支持提前，且海运节点(5)会断链。

对应缺口矩阵：

| 业务域 | 已具备 | 关键缺口(P0) | 增强(P1) |
| :-- | :-- | :-- | :-- |
| 货物·班轮 | 装箱单/大副收据/吊装备注 | 货物台账、箱封号、船名航次、船舶动态 | 散货/Omnibus 参数 |
| 商务·财务 | 保险申购单/税费凭证提醒 | Incoterms·付款条款、费用估算、汇率 | 成本毛利核算 |
| 时间·风险 | 12节点串行排程、缓冲预警、状态机 | 全环节推迟/提前、ETA联动重排、瓶颈高亮、工作日感知 | 风险SLA可配置、节假日库 |
| 协作·跨组织 | 角色泳道、责任部门 | 单证附件存档、数据导出 | 多用户权限/审批 |
| 知识·扩展 | 巴西模板、青岛港子配置 | 更多国家/港口模板 | 多语言、单证有效期提醒 |

> 本方案**聚焦前三个业务域中与「货物 + 时间动态」直接相关的部分**（D1、D2、D3），并缩小到可落地的最小闭环。

---

## 2. D1 货物 · 班轮维度（P0，最缺失）

### 2.1 货物台账 `cargo_items`

新增一张表，挂在 `projects` 下，回答"这批货到底是什么"：

```sql
CREATE TABLE IF NOT EXISTS cargo_items (
    item_id      TEXT PRIMARY KEY,        -- UUID
    project_id   TEXT NOT NULL,
    seq          INTEGER,                 -- 项号/件号
    item_name    TEXT NOT NULL,           -- 设备名
    qty          INTEGER DEFAULT 1,
    unit         TEXT,                    -- 台 / 套 / 件
    dim_l        REAL, dim_w REAL, dim_h REAL,  -- 单件尺寸 L×W×H (m)
    weight_kg    REAL,                    -- 单件毛重 (kg)
    gross_m3     REAL,                    -- 单件体积 (m³)
    over_flag    INTEGER DEFAULT 0,       -- 是否超限
    od_type      TEXT,                    -- 超宽/超高/超长/超重 明细
    marks        TEXT,                    -- 唛头 Marks & Numbers
    packaging    TEXT,                    -- 木箱 / 裸装 / 绑扎
    container_no TEXT, seal_no TEXT,      -- 装箱后登记
    FOREIGN KEY (project_id) REFERENCES projects(project_id)
);
```

**价值不止记录，而是能跑校验逻辑，呼应巴西模板里的吊装约束：**

- 单件 `weight_kg > 100000`（100t）→ 节点3「捆扎固定」自动标红"吊装能力预警"（对应 config 中 `吊机≥100t/臂长≥36m`）。
- `gross_m3` 作为临时堆存、舱位估量的输入。
- 甘特节点可显示"本节点涉及项号"，把时间轴与货物本体连接。

### 2.2 箱/封号 与 船名航次

- **箱/封号**：放 `cargo_items`（装箱后登记），装箱后可一键导出给船公司/口岸。
- **船名/航次**：新增轻量 `vessel` 表（1 项目 1 船），`projects` 加 `vessel_id` 外键：

```sql
CREATE TABLE IF NOT EXISTS vessel (
    vessel_id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL,
    vessel_name TEXT,   -- 船名
    imo        TEXT,    -- IMO number
    voyage     TEXT,    -- 航次
    carrier    TEXT,    -- 承运人
    mmsi       TEXT     -- 预留，便于未来接 AIS
);
```

### 2.3 船舶动态 —— 抽象留口，但只做实「手动」

**决定：联网/实时 AIS 不做。** 因此不接第三方 API，仅保留抽象接口便于将来扩展，当前只实现 `ManualProvider`（操作员登记船位/实际 ETA），历史写入 `vessel_positions` 本地表以便回放：

```python
# services/vessel_status.py
from abc import ABC, abstractmethod

class VesselStatusProvider(ABC):
    @abstractmethod
    def fetch(self, vessel) -> dict | None: ...

class ManualProvider(VesselStatusProvider):
    # 离线：操作员手动登记 lat/lon、实际ETA、备注
    def fetch(self, vessel): ...

# 现状只启用 ManualProvider；未来如需 AIS，再实现 AisProvider 并按优先级串联
PROVIDERS = [ManualProvider()]
```

### 2.4 落地 UI 要点

- 新建项目页新增"货物台账"折叠区，可逐条增删货项；载入巴西模板时可选预填常用件杂货字段。
- 主看板折叠卡片右侧微统计加一项"货物 N 项/超限 M"。
- 装船/堆存节点行可弹出货物清单。

---

## 3. D2 时间 · 风险维度（P0）

### 3.1 全环节推迟 / 提前 —— `shift_node`（本次核心）

> 现有 `delay_nodes` 的三大缺陷：**① 只支持推迟（正天数）**；**② 海运节点(5)会断链**（其 start 不再等于境内末 ETD、end 不再等于境外首 ETA）；**③ `delay_days` 只能累积正数，无法表达"提前"也无法回退。**

**改进：统一为 `shift_node(nodes, project, from_node_id, days)`，`days` 可正可负（正=推迟，负=提前），覆盖全部 12 节点。** 传播规则按"所选节点所在区域"分三类：

| 选中节点 | 受影响范围 | 联动字段 | 语义 |
| :-- | :-- | :-- | :-- |
| 境内 1–4 | 本节点 → 节点4 + 海运(5) | `etd` = 海运 start | 前段（未完成节点）留"窗口/松弛"，不硬推 |
| 海运 5 | 海上 + 境外全段(6–12) | `eta`，海运 `duration=eta−etd` 重算 | 船期顺延/提前驱动整条境外段 |
| 境外 6–12 | 本节点 → 节点12 | `eta`（仅当动 node 6 时） | 口岸操作整段平移 |

**核心原则：三条子链锚点分离、互不污染**——境内锚定 ETD、境外锚定 ETA；对境内操作绝不动境外，对境外操作绝不动境内，海运是两边锚点的桥。

**代码骨架：**

```python
# services/scheduler.py
AREAS = ["DOME", "SEA", "OVERSEA"]

def shift_node(nodes, project, from_node_id, days):
    """days>0 推迟；days<0 提前。返回 (nodes, new_etd, new_eta)。"""
    node_map = {n["node_id"]: n for n in nodes}
    fr = node_map[from_node_id]
    area = fr["area"]

    if area == "DOME":
        # 本节点起..节点4，再带上海运(5)：受影响 = [from..4] + 海运
        for n in nodes:
            if (n["area"] == "DOME" and n["seq"] >= fr["seq"]) or n["area"] == "SEA":
                _shift(n, days)
        new_etd, new_eta = _recalc_boundary(nodes)
    elif area == "SEA":
        # 海运 → 海上 + 境外全段；ETA 联动，海运 duration 重算
        for n in nodes:
            if n["area"] in ("SEA", "OVERSEA"):
                _shift(n, days)
        new_etd, new_eta = _recalc_boundary(nodes)
    else:  # OVERSEA
        # 境外本节点起..12
        for n in nodes:
            if n["area"] == "OVERSEA" and n["seq"] >= fr["seq"]:
                _shift(n, days)
        new_etd, new_eta = _recalc_boundary(nodes)

    _guard(nodes, project)   # 见 3.2
    return nodes, new_etd, new_eta
```

### 3.2 四个安全守卫（提前比推迟更需要）

1. **不早于今日 / 不覆盖已完成**：位移后任一受影响节点 `plan_start < today`（且非 Done）或撞到 `actual_completion_date` → 拦截。
2. **Done 节点保护**：已完成节点不参与位移，若被撞到则报错。
3. **链连续性校验**：位移后重建衔接，校验 `境内末 == 海运 start`、`海运 end == 境外首 start`，不允许海运锚点漂移。
4. **可回退（撤销）**：`delay_days` 记净位移（可负）；新增 `shift_history` 记录（`project_id, node_id, delta, ts, note`）以便"提前过头"一键还原。

```sql
-- 新增位移历史表（回退用）
CREATE TABLE IF NOT EXISTS shift_history (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id  TEXT NOT NULL,
    node_id     INTEGER NOT NULL,
    delta       INTEGER NOT NULL,        -- 正值推迟 / 负值提前
    created_at  TEXT NOT NULL
);
```

### 3.3 外生 ETA 变化 → 境外段重排（手动简化版）

**联网不做 → 收敛为一个手动语义**：用户直接改 ETA（或把海运/境外节点提前、推迟）→ 触发一次以 ETA 为锚的境外段重排。实现即可复用 `shift_node` 的"锚点 + 平移"内核，不必再为外部船期设计接口。

### 3.4 瓶颈 / 松弛高亮（替代教材式 CPM 的务实做法）

> 诚实判断：本流程**纯串行**（境内→海运→境外，无并行分支），教科书 CPM（并行 slack）没有增量价值。退一步做"瓶颈 + 风险"高亮更实用：

- **瓶颈**：节点 `duration` 占全流程比例 > 阈值（如海运 45/62≈73%）→ 标"最长段"。
- **松弛**：将 4 天缓冲视为整条境外段的 slack；节点9/10 一旦逾期即提示"已消耗缓冲 X 天"（复用现有 §4.3 逻辑）。
- **风险**：结合 `status`（Overdue / 缓冲被吃）在甘特上标色。

组合效果：一眼看出"海运最长、查验/堆存吃缓冲"。

### 3.5 联动（必须一并做）

位移/重排后自动触发：
1. **单证 due_date 重算**（`files.due_date` 锚在 `node_start/node_end`）——复用 `file_checklist.compute_file_due`。
2. **甘特/主看板全局刷新**——与 `reminder.py` 全量重算 + 去重同一条链路。

---

## 4. 明确不做（范围边界）

| 项 | 原因 |
| :-- | :-- |
| 实时 AIS / 船司船期抓取 | 纯本地离线，联网冲突；动态靠人工标注 |
| 多用户 / 权限 / 审批流 | 与"单机桌面工具"定位不符，且改动面大 |
| 教材式 CPM | 串行流程无增量价值，改做瓶颈/松弛高亮 |

---

## 5. 落地节奏与任务拆分

| 档位 | 内容 | 依赖 | 预计 |
| :-- | :-- | :-- | :-- |
| **快赢** | `cargo_items` 表 + 台账录入界面；`shift_node` 全环节推迟/提前 + 四守卫；甘特瓶颈/风险高亮 | 无 | 约 3–4 天 |
| **承接** | `vessel` 表 + 船位手动登记 + `eta_source` 联动闭环；单证 due 跟随重算 | 快赢 | 约 2 天 |
| **增强(P1)** | 更多国家/港口模板扩展、数据导出、`Provider` 留的 AIS 口（暂不启用） | 承接 | 按需 |

> 排期建议：先做「快赢」，跑通"任意节点推迟/提前 → 下游与 ETA 联动 → 单证同步"的主线，再补 `vessel` 动态层。

---

## 6. 与现有代码衔接

| 现有文件 | 改动 |
| :-- | :-- |
| `db.py` | 新增 `cargo_items`、`vessel`、`shift_history` 三表 + CRUD |
| `services/scheduler.py` | `delay_nodes` → `shift_node`（可正负、覆盖全环节、四守卫） |
| `services/vessel_status.py` | **新增**：Provider 抽象 + `ManualProvider`（只做实手动） |
| `services/file_checklist.py` | `compute_file_due` 复用；位移后重算 due |
| `ui/pages/new_project_page.py` | 新增"货物台账"折叠区 |
| `ui/widgets/gantt_grid.py` | 每节点行新增 `+ 推迟 / 提前 −` 控件；瓶颈/风险高亮 |
| `config.py` | 不强制改；吊装阈值可复用模板备注 |

> 分层约定不变：UI → services → db（+config），UI 不直接写库。

---

## 7. 验收要点

| # | 用例 | 期望 |
| :-: | :-- | :-- |
| S1 | 新建项目录入 2 条货物台账（1 条 >100t 超重） | 台账入 `cargo_items`；超重项在节点3标红"吊装预警" |
| S2 | 对境外节点⑥ 推迟 3 天 | 节点6–12 顺延 +3，境内与海运不变；`eta` 不变 |
| S3 | 对境内节点② 提前 2 天 | 节点2–4 + 海运提前；ETD 联动；境外不动 |
| S4 | 对海运节点⑤ 推迟 5 天 | 海上 + 境外全段顺延 +5；`eta` = 原 ETA +5；海运 `duration` 重算 |
| S5 | 提前到 < 今日 或 撞已完成节点 | 被拦截并提示 |
| S6 | 位移后单证 due | 锚 `node_start/node_end` 的单证 due 同步重算 |
| S7 | `delay_days` 呈现净位移 | 负值（提前）正确显示；`shift_history` 留痕可回退 |
| S8 | 甘特瓶颈/风险高亮 | 海运标"最长段"；节点9/10 逾期提示"已消耗缓冲" |

---

*本文档为方案，落地以「快赢」档为下一个交付目标。*