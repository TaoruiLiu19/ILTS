# 国际物流全流程跟踪系统 · 完整开发文档（PRD v5.0）

> 版本：v5.0 | 更新日期：2026-09-08 | 状态：可交付开发


## 目录

1. [项目概述](#1-项目概述)
2. [技术栈建议](#2-技术栈建议)
3. [数据模型（LocalStorage Schema）](#3-数据模型localstorage-schema)
4. [页面一：项目启动页（首页）](#4-页面一项目启动页首页)
5. [页面二：主看板（进行中项目）](#5-页面二主看板进行中项目)
6. [页面三：已完成项目列表 & 只读甘特图](#6-页面三已完成项目列表--只读甘特图)
7. [页面四：新建项目录入页](#7-页面四新建项目录入页)
8. [核心算法规范](#8-核心算法规范)
9. [全局规则与边界处理](#9-全局规则与边界处理)
10. [UI视觉规范速查](#10-ui视觉规范速查)
11. [开发排期建议](#11-开发排期建议)


## 1. 项目概述

### 1.1 产品定位

面向国际货运代理/外贸操作员的项目全生命周期管理工具，提供从**项目录入 → 过程跟踪 → 预警提醒 → 完结归档**的闭环管理。

### 1.2 核心设计原则

| 原则 | 说明 |
| :--- | :--- |
| **一页总览** | 启动页统管所有项目入口 |
| **多项目并行** | 主看板支持同时跟踪多个进行中项目 |
| **只读归档** | 已完成项目可查阅但不可编辑，不触发预警 |
| **海运压缩** | 海上运输段在所有视图中自动压缩，控制横向宽度 |
| **纯本地运行** | 所有数据存储于浏览器 LocalStorage，无后端 |

### 1.3 页面路由总览

| 路由路径 | 页面名称 | 功能简述 |
| :--- | :--- | :--- |
| `/` | 项目启动页（首页） | 三大功能入口卡片 |
| `/active` | 主看板 | 进行中项目折叠卡片列表 + 操作 |
| `/completed` | 已完成项目列表 | 所有已完结项目的列表 |
| `/completed/:id` | 只读甘特图详情页 | 单个已完成项目的只读甘特图 |
| `/new` | 新建项目录入页 | 录入基础信息 + 耗时调整 + 保存 |


## 2. 技术栈建议

| 层级 | 技术选型 | 说明 |
| :--- | :--- | :--- |
| 框架 | React 18+ / Vue 3+ | 任选，推荐 React + TypeScript |
| 路由 | React Router v6 / Vue Router 4 | 支持多页面独立路由 |
| 样式 | CSS Modules / TailwindCSS | 建议 Tailwind，快速实现卡片/网格布局 |
| 状态管理 | Zustand / Pinia / Context | 仅需管理当前选中的项目 ID |
| 数据持久化 | LocalStorage | 键名：`logistics_projects` |
| 日期处理 | Day.js | 轻量，适合日期加减/比较 |
| UI 组件库 | 不强制 | 建议使用 shadcn/ui 或 Ant Design 的 Table / DatePicker / Modal |


## 3. 数据模型（LocalStorage Schema）

### 3.1 根数据结构

```typescript
// localStorage 键名: 'logistics_projects'
interface RootStorage {
  allProjects: Project[];
  uiPreferences: {
    lastOpenedProjectId: string | null;  // 上次打开的项目 ID
    lastVisitedPage: '/' | '/active' | '/completed' | '/new';
  };
}
```

### 3.2 单个项目数据结构

```typescript
type ProjectStatus = 'Active' | 'Completed' | 'Draft';
type NodeStatus = 'Pending' | 'Active' | 'Overdue' | 'Done';

interface Project {
  projectId: string;                 // UUID v4
  projectName: string;               // 项目名称（必填）
  status: ProjectStatus;             // 项目状态
  etd: string;                       // 预计离港日 'YYYY-MM-DD'
  eta: string;                       // 预计到港日 'YYYY-MM-DD'
  actualCompletionDate: string | null; // 实际完结日期，仅 Completed 时填充
  createDate: string;                // 创建日期 'YYYY-MM-DD'
  lastAlertDate: string;             // 最后一次弹窗预警日期，用于去重
  nodes: Node[];
}

interface Node {
  nodeId: 1 | 2 | 3 | 4 | 5 | 6 | 7; // 固定7个节点
  nodeName: string;                  // 节点中文名
  planStart: string;                 // 计划启动日 'YYYY-MM-DD'
  planEnd: string;                   // 计划截止日 'YYYY-MM-DD'
  duration: number;                  // 耗时（天数，用户可调）
  isDelayed: boolean;                // 是否被人工推迟过
  delayDays: number;                 // 累计推迟天数
  files: string[];                   // 所需文件清单
  status: NodeStatus;               // 节点状态
  actualCompletionDate: string | null; // 实际完成日期，Done 时填充
}
```

### 3.3 7个节点的固定配置

| nodeId | nodeName | 默认耗时 | 所属区域 | 锚点依赖 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 出口报关 | 3天 | 境内段 | ETD - 3天 |
| 2 | 集港/捆扎 | 1天 | 境内段 | ETD - 1天 |
| 3 | 海上运输 | 动态计算 | 海运段 | ETD → ETA |
| 4 | 到港换单 | 1天 | 境外段 | ETA + 1天 |
| 5 | 进口清关 | 3天 | 境外段 | ETA + 3天 |
| 6 | 海关查验/放行 | 1天 | 境外段 | 清关截止 + 1天 |
| 7 | 内陆运输/交付 | 2天 | 境外段 | 查验截止 + 2天 |


## 4. 页面一：项目启动页（首页）

### 4.1 页面路由
- **路径**：`/`
- **访问**：打开软件默认显示

### 4.2 布局结构

```
┌──────────────────────────────────────────────────────┐
│  🚢 国际物流跟踪系统                    📅 2026-09-08 │  ← 顶部导航栏
│  ⚠️ 今日有 2 个项目存在截止/逾期                      │  ← 全局提示条
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │  🔄          │  │  📂          │  │  ➕          │ │
│  │ 进行中的项目  │  │ 查看已完成   │  │ 新建项目    │ │
│  │  (3)         │  │  (5)         │  │             │ │
│  │              │  │              │  │             │ │
│  │ 迪拜光伏     │  │ 美国芝加哥   │  │ 输入ETD/ETA │ │
│  │  进口清关中   │  │  2026-08-15 │  │ 自动生成    │ │
│  │  距ETD 5天   │  │ 上海-伦敦   │  │ 时间轴     │ │
│  │ 新加坡-悉尼  │  │  2026-08-10 │  │             │ │
│  │  海上运输中   │  │  ...        │  │             │ │
│  │  距ETD 23天  │  │              │  │             │ │
│  │              │  │              │  │             │ │
│  │ [+2 更多...] │  │              │  │             │ │
│  │              │  │              │  │             │ │
│  │ [▶ 进入主看板]│  │ [📋查看全部归档│  │ [✏️ 创建项目]│ │
│  └─────────────┘  └─────────────┘  └─────────────┘ │
│                                                      │
│  © 2026 国际物流跟踪系统 v1.0                        │  ← 底部版权
└──────────────────────────────────────────────────────┘
```

### 4.3 卡片详细规格

#### 卡片 A：进行中的项目

| 元素 | 规格 |
| :--- | :--- |
| 图标 | 🔄 带旋转动画（CSS animation） |
| 标题 | “进行中的项目” |
| 数量徽章 | 右上角红色圆点，显示 Active 项目数量 |
| 预览列表 | 最多显示 3 个项目，每行显示：项目名 + 当前节点名 + “距ETD X天” |
| 超过 3 个 | 底部显示 “+ N 个更多项目...” |
| 主按钮 | `▶ 进入主看板`，主色调（蓝色），大号 |
| 空状态 | 显示 “🎉 暂无进行中的项目” + “去新建一个吧 →”（可点击跳转 `/new`） |

#### 卡片 B：查看已完成项目

| 元素 | 规格 |
| :--- | :--- |
| 图标 | 📂 |
| 标题 | “查看已完成项目” |
| 数量徽章 | 灰色圆点，显示 Completed 项目数量 |
| 预览列表 | 最多显示 3 个项目，每行显示：项目名 + “YYYY-MM-DD 完结” |
| 主按钮 | `📋 查看全部归档`，次要色调（灰色/墨色） |
| 空状态 | 显示 “暂无已完成项目” |

#### 卡片 C：新建项目

| 元素 | 规格 |
| :--- | :--- |
| 图标 | ➕ 大号，主色 |
| 标题 | “新建项目” |
| 描述 | “输入 ETD / ETA，系统自动生成完整时间轴” |
| 主按钮 | `✏️ 创建新项目`，成功色调（绿色），大号 |

### 4.4 交互规则

| 触发 | 行为 |
| :--- | :--- |
| 页面加载 | 读取 LocalStorage，分别统计 Active / Completed 数量并渲染 |
| 点击卡片 A 主按钮 | 跳转至 `/active` |
| 点击卡片 B 主按钮 | 跳转至 `/completed` |
| 点击卡片 C 主按钮 | 跳转至 `/new` |
| 顶部提示条 | 遍历所有 Active 项目，统计今日截止或逾期的节点数量，显示文字提示（仅提示，不弹窗） |


## 5. 页面二：主看板（进行中项目）

### 5.1 页面路由
- **路径**：`/active`
- **访问**：从启动页点击“进入主看板”跳转，或从录入页保存成功后自动跳转

### 5.2 布局结构

```
┌──────────────────────────────────────────────────────────────────────┐
│  ← 返回启动页    进行中项目                          [+ 新建] 📂   │  ← 顶部导航
│  ────────────────────────────────────────────────────────────────── │
│  📊 进行中 3 个  ·  ⏰ 今日截止 1 个  ·  🚨 逾期 2 个            │  ← 统计栏
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │ 🔴 [项目A] 迪拜光伏组件运输                    [⏬ 展开详情]   │ │  ← 卡片折叠
│  │  🟡 进行中  ·  当前：进口清关  ·  距ETD 5天  ·  3/7 ✅        │ │
│  │  ████████████████🚩████████████████🏁██████████              │ │  ← 微型色块条
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │ 🟢 [项目B] 新加坡-悉尼特种柜                  [⏬ 展开详情]   │ │  ← 卡片折叠
│  │  🟢 正常  ·  当前：海上运输  ·  距ETD 23天  ·  2/7 ✅        │ │
│  │  ████████████████🚩████████████████🏁██████████              │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │ 🟡 [项目C] 上海-伦敦拼箱                    [⏬ 展开详情]     │ │  ← 卡片展开
│  │  🟡 进行中  ·  当前：出口报关  ·  距ETD 2天  ·  1/7 ✅        │ │
│  │  ████████████████🚩████████████████🏁██████████              │ │
│  │ ┌─────────────────────────────────────────────────────────────┐ │ │
│  │ │  境内出口段(冰川蓝)  │ 海上运输段(深海蓝) │ 境外进口段(沙土黄)│ │ │  ← 区域标识行
│  │ │  出口报关  ████████  │ 海上运输 ██████████│ 到港换单 ████   │ │ │  ← 日历网格
│  │ │  集港捆扎  ████████  │ (省略号压缩)       │ 进口清关 ████   │ │ │
│  │ │                    │                    │ 查验放行 ████   │ │ │
│  │ │                    │                    │ 内陆交付 ████   │ │ │
│  │ └─────────────────────────────────────────────────────────────┘ │ │
│  │  [✅ 项目已完成，结束跟踪]                                     │ │  ← 底部按钮
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  💡 已显示全部 3 个进行中项目                                       │  ← 底部提示
└──────────────────────────────────────────────────────────────────────┘
```

### 5.3 顶部导航栏

| 元素 | 规格 |
| :--- | :--- |
| 返回按钮 | `← 返回启动页`，点击跳转至 `/` |
| 标题 | “进行中项目” |
| 快捷新建 | `[+ 新建]` 按钮，点击跳转至 `/new` |
| 归档入口 | `📂` 图标按钮，点击跳转至 `/completed`（快捷入口） |

### 5.4 统计栏

- 自动计算并显示：
  - “进行中 N 个”——所有 Active 项目总数
  - “今日截止 N 个”——今日有节点截止的项目数（去重）
  - “逾期 N 个”——今日有节点逾期的项目数（去重）

### 5.5 项目卡片（折叠状态）—— 高度 120px

| 区域 | 宽度占比 | 内容 |
| :--- | :--- | :--- |
| **左区** | 25% | 项目名称（加粗，16px）+ 状态标签（🟡进行中 / 🔴有逾期 / 🟢正常）+ 项目 ID（小字灰色） |
| **中区** | 50% | 微型三区色块条（境内冰川蓝→🚩ETD→海运深海蓝→🏁ETA→境外沙土黄），下方显示“当前：{节点名}” |
| **右区** | 25% | “距ETD X天” + “进度 X/7 ✅” + `⏬ 展开详情` 按钮 |

**预警外显**：

| 条件 | 视觉变化 |
| :--- | :--- |
| 存在逾期节点 | 卡片左侧 5px 红色竖条 + 卡片背景 `#FFF5F5` + 状态标签显示 🔴有逾期 |
| 存在今日截止节点（无逾期） | 卡片左侧 5px 橙色竖条 + 状态标签显示 🟡进行中 |
| 一切正常 | 卡片左侧 5px 绿色竖条 + 状态标签显示 🟢正常 |

**微型色块条规格**：
- 高度 20px，圆角 4px
- 境内段：冰川蓝 `#F0F7FF`，宽度按（境内段总天数 / 项目总天数）比例
- 海运段：深海蓝 `#D4E8FF`，宽度按（海运总天数 / 项目总天数）比例
- 境外段：沙土黄 `#FFF7E8`，宽度按（境外段总天数 / 项目总天数）比例
- ETD 标记：🚩 红色小旗，位置在境内段与海运段交界
- ETA 标记：🏁 绿色小旗，位置在海运段与境外段交界
- 当前日期位置：在色块条上以一条细竖线 + 小圆点表示“今日”

### 5.6 项目卡片（展开状态）—— 高度约 550~650px

点击 `⏬ 展开详情` 后，卡片展开显示完整日历网格。

#### 5.6.1 区域标识行（第一行）

位于日历网格顶部，高度 36px，将 X 轴切割为三段：

| 区域 | 背景色 | 文字标签 | 覆盖列范围 |
| :--- | :--- | :--- | :--- |
| 境内段 | 冰川蓝 `#F0F7FF` | “🚢 境内出口段” | 从首列 → ETD 列 |
| 海运段 | 深海蓝 `#D4E8FF` | “⛵ 海上运输段” | 从 ETD 列 → ETA 列 |
| 境外段 | 沙土黄 `#FFF7E8` | “🏗️ 境外进口段” | 从 ETA 列 → 末列 |

#### 5.6.2 日历网格主体

- **行（Y轴）**：固定 7 行，对应 7 个节点，行高 48px
- **列（X轴）**：从第一个节点的 `planStart` 到最后一个节点的 `planEnd`，每天一列
- **列头**：显示日期（如 `09/07`），周末日期用灰色文字
- **单元格分层渲染**：
  1. **底层**：整列背景填充区域色（境内/海运/境外）
  2. **中层**：在节点时间窗口内的格子，覆盖半透明状态色块（圆角 2px）
     - `Pending`：不覆盖，保留区域底色
     - `Active`：金色半透明 `rgba(255, 193, 7, 0.5)` + 外发光
     - `Overdue`：红色半透明 `rgba(244, 67, 54, 0.5)`
     - `Done`：绿色半透明 `rgba(76, 175, 80, 0.4)`
  3. **顶层**：文字信息（节点名称缩写、剩余天数等）

#### 5.6.3 海运压缩规则（关键）

当海运段总天数 > 25 天时，触发压缩：

- **显示策略**：海运段的 X 轴列数压缩为 **不超过 7 列**
  - 第 1 列：`ETD` 日期
  - 第 2 列：`ETD + 1天`
  - 第 3 列：`…`（占位，宽度 40px）
  - 第 4 列：`今日` 日期（若今日在海运段内）
  - 第 5 列：`…`（占位，宽度 40px）
  - 第 6 列：`ETA - 1天`
  - 第 7 列：`ETA` 日期
- **压缩列背景**：始终保持深海蓝 `#D4E8FF`
- **悬停交互**：鼠标悬停在 `…` 列上 → Tooltip 显示 “已省略 X 天（共航行 Y 天）”
- **不触发压缩**：境内段和境外段总天数较短（通常 ≤ 5 天），始终显示完整日期列

#### 5.6.4 节点行操作

每行右侧固定列（宽度 140px）包含：

| 元素 | 说明 |
| :--- | :--- |
| 状态指示灯 | 🔴逾期 / 🟡进行中 / 🟢完成 / ⚪未开始 |
| 剩余天数/进度 | “剩余 3 天” 或 “已完成” |
| `✓ 标记完成` 按钮 | 仅当节点状态为 Active 或 Overdue 时显示 |
| `⏩ 推迟` 按钮 | 仅当节点状态为 Pending 或 Active 时显示（Completed 节点不可推迟） |

#### 5.6.5 文件清单悬停

- 节点名称旁显示 `📎` 图标
- 鼠标悬停 → Tooltip 弹出显示该节点的 `files` 数组内容，每行一个文件

#### 5.6.6 卡片底部：结束项目按钮

- 显示条件：所有 7 个节点状态均为 `Done`
- 样式：绿色主按钮 `✅ 项目已完成，结束跟踪`
- 点击行为：
  1. 弹出二次确认模态框：
     - 标题：“确认结束项目？”
     - 正文：“项目【{项目名}】所有节点已完成，确定结束跟踪吗？结束后将移至归档。”
     - 按钮：`取消`（灰色） / `确认结束`（红色）
  2. 确认后：
     - 项目 `status` 改为 `Completed`
     - `actualCompletionDate = Today`
     - 卡片从主看板列表移除
     - 顶部弹出 Toast：`🎉 项目已完结，已移至归档区。`

### 5.7 节点状态机（核心逻辑）

每日自动计算，基于 `Today` 与 `planStart` / `planEnd` 对比：

| 状态 | 指示灯 | 触发条件 | UI 表现 |
| :--- | :--- | :--- | :--- |
| Overdue | 🔴 红色 | `Today > planEnd` 且 `status ≠ Done` | 节点行红色背景，右侧显示“已逾期 X 天” |
| Active | 🟡 黄色 | `planStart ≤ Today ≤ planEnd` 且 `status ≠ Done` | 节点行金色外发光，右侧显示“▶ 进行中” |
| Done | 🟢 绿色 | 用户手动点击“标记完成” | 节点行绿色背景，右侧显示“✅ 已完成” |
| Pending | ⚪ 灰色 | `Today < planStart` | 节点行半透明，右侧显示“⏳ 未开始” |

### 5.8 “推迟”功能（连锁推迟）

| 步骤 | 交互 |
| :--- | :--- |
| 1 | 点击节点右侧 `⏩ 推迟` 按钮 |
| 2 | 弹出模态框：“当前节点【{节点名}】计划截止日为 {planEnd}。请输入推迟天数（1-30）：” + 数字输入框 |
| 3 | 用户输入正整数 N，点击“确认推迟” |
| 4 | 系统执行级联更新：从当前节点开始，到数组末尾，所有节点的 `planStart` 和 `planEnd` 均 + N 天 |
| 5 | 若推迟的是境内段节点（nodeId 1-2），项目的 `etd` 同步 + N 天，海上运输的 `planStart` 相应更新 |
| 6 | 若推迟的是境外段节点（nodeId 4-7），项目的 `eta` 同步 + N 天 |
| 7 | 被推迟的节点标记 `isDelayed = true`，`delayDays += N`，节点名称旁显示 🕒 图标，悬停显示“已人工推迟 N 天” |
| 8 | 顶部弹出 Toast：`✅ 已成功推迟【{节点名}】及后续所有节点 N 天` |


## 6. 页面三：已完成项目列表 & 只读甘特图

### 6.1 列表页路由
- **路径**：`/completed`
- **访问**：从启动页点击“查看全部归档”跳转

### 6.2 列表页布局

```
┌──────────────────────────────────────────────────────────────┐
│  ← 返回启动页    已完成项目（归档）              🔍 [搜索框] │  ← 顶部导航
├──────────────────────────────────────────────────────────────┤
│  📊 共 5 个已完成项目                                      │  ← 统计
├──────────────────────────────────────────────────────────────┤
│  项目名称          ETD         ETA       完成日期     总耗时 │  ← 表头
│  ────────────────────────────────────────────────────────── │
│  迪拜光伏组件运输  2026-09-10  2026-10-25  2026-10-28  47天 │  ← 可点击行
│  美国芝加哥特种柜  2026-07-15  2026-08-12  2026-08-15  31天 │
│  上海-伦敦拼箱    2026-06-01  2026-07-20  2026-07-22  51天 │
│  ...                                                     │
└──────────────────────────────────────────────────────────────┘
```

### 6.3 列表交互

| 元素 | 规格 |
| :--- | :--- |
| 搜索框 | 按项目名称模糊匹配，实时筛选 |
| 表格行 | 鼠标悬停高亮，点击行进入该项目的只读甘特图详情页 |
| 空状态 | 显示“📭 暂无已完成项目” + 引导按钮“去新建一个” |

### 6.4 只读甘特图详情页路由
- **路径**：`/completed/:projectId`
- **访问**：从已完成列表点击任意行跳转

### 6.5 只读甘特图详情页布局

```
┌────────────────────────────────────────────────────────────────────┐
│  ← 返回已完成列表   迪拜光伏组件运输     📂 只读模式 · 已归档   │  ← 顶部导航 + 横幅
├────────────────────────────────────────────────────────────────────┤
│  项目信息：ETD: 2026-09-10  |  ETA: 2026-10-25  |  实际完结: 2026-10-28 │
├────────────────────────────────────────────────────────────────────┤
│                                                                   │
│  出口报关    ████████████         实际完成: 2026-09-10           │
│  集港/捆扎   ████████████         实际完成: 2026-09-10           │
│  海上运输    ████ … ████ … ████  实际完成: 2026-10-25  ← 压缩   │
│  到港换单                ██████   实际完成: 2026-10-26           │
│  进口清关                ██████   实际完成: 2026-10-26           │
│  海关查验                ██████   实际完成: 2026-10-27           │
│  内陆运输                ██████   实际完成: 2026-10-28           │
│                                                                   │
│  09/01  09/08  09/15  …  10/01  …  10/15  10/22  10/29         │
│                                                                   │
│  💡 只读模式 · 所有节点已完成或未执行，无预警，不可编辑          │
└────────────────────────────────────────────────────────────────────┘
```

### 6.6 只读甘特图详细规格

#### 6.6.1 顶部横幅（强制显示）
- 黄色背景 `#FFF8E1`，高度 40px
- 文字：`📂 只读模式 · 该项目已于 YYYY-MM-DD 完结，仅作历史查看，不触发预警。`

#### 6.6.2 项目信息行
- 显示：ETD、ETA、实际完结日期

#### 6.6.3 甘特图主体
- **条形图样式**：每个节点一行，左侧显示节点名称，右侧显示横向时间条
- **节点状态固定**：
  - 若 `actualCompletionDate` 有值 → 🟢 绿色条，右侧显示“实际完成于：YYYY-MM-DD”
  - 若为 null（未执行）→ ⚪ 灰色条，右侧显示“未执行”
- **不显示红色逾期**：即使 `Today > planEnd`，在只读视图中也**不显示红色**
- **全部操作按钮隐藏**：`✓ 标记完成`、`⏩ 推迟`、`✅ 结束项目` 全部不显示

#### 6.6.4 海运压缩（与主看板规则一致）
- 海运节点条在 X 轴上的视觉宽度 **不超过 7 个标准日期列宽度**
- 采用 `…` 省略号压缩
- 悬停显示 “已省略 X 天（共航行 Y 天）”

#### 6.6.5 无预警
- 页面加载时，系统检测到 `status === 'Completed'`，**完全跳过**预警弹窗逻辑


## 7. 页面四：新建项目录入页

### 7.1 页面路由
- **路径**：`/new`
- **访问**：从启动页点击“创建新项目”跳转，或从主看板点击“+ 新建”跳转

### 7.2 布局结构

```
┌──────────────────────────────────────────────────────────────┐
│  ← 返回启动页    新建物流项目                    [草稿箱]   │  ← 顶部导航
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  项目名称  [___________________________]                    │  ← 基础信息
│  预计离港日 [📅 2026-09-10]  预计到港日 [📅 2026-10-25]   │
│                                                              │
│  ── ▼ 高级排程参数（点击展开/折叠） ──                     │
│                                                              │
│  节点名称              耗时（天）  [+][-]                   │
│  ① 出口报关            [  3  ]    ➖  ➕                    │  ← 可调
│  ② 集港/捆扎           [  1  ]    ➖  ➕                    │
│  ③ 海上运输            [ 45  ]    ➖  ➕    ← ETA-ETD 自动计算 │
│  ④ 到港换单            [  1  ]    ➖  ➕                    │
│  ⑤ 进口清关            [  3  ]    ➖  ➕                    │
│  ⑥ 海关查验/放行       [  1  ]    ➖  ➕                    │
│  ⑦ 内陆运输/交付       [  2  ]    ➖  ➕                    │
│                                                              │
│  ⚠️ 调整耗时后，系统将自动重算所有节点的起止日期             │  ← 提示
│                                                              │
│  [取消]                          [🚀 生成时间轴并保存]      │  ← 底部按钮
└──────────────────────────────────────────────────────────────┘
```

### 7.3 表单字段规格

| 字段 | 类型 | 必填 | 校验规则 |
| :--- | :--- | :--- | :--- |
| 项目名称 | Input text | 是 | 不可为空，最长 50 字符 |
| ETD | DatePicker | 是 | ≥ 今日，格式 YYYY-MM-DD |
| ETA | DatePicker | 是 | > ETD，格式 YYYY-MM-DD |

### 7.4 高级排程参数（可折叠面板）

- **默认展开**：首次进入时默认展开
- **每个节点**：显示节点序号 + 名称 + 数字输入框（min=1，max=999）+ `−` `+` 按钮
- **海上运输特殊处理**：
  - 默认值 = `ETA - ETD`（天数差）
  - 用户修改时，系统弹出提示：“修改海上运输耗时将同步更新 ETA，是否继续？” → 确认后，ETA 自动变为 `ETD + 新耗时`
- **联动重算**：任何节点的耗时变化，点击保存时统一重算所有 `planStart` / `planEnd`

### 7.5 保存与跳转

| 步骤 | 动作 |
| :--- | :--- |
| 1 | 用户点击 `🚀 生成时间轴并保存` |
| 2 | 前端校验：项目名称非空、ETD ≥ Today、ETA > ETD、所有耗时 ≥ 1 |
| 3 | 校验失败 → 表单标红 + 错误提示 Toast |
| 4 | 校验成功 → 执行排程算法（见第 8 节），生成 7 个节点的完整数据 |
| 5 | 生成 UUID，`status = 'Active'`，`createDate = Today`，`lastAlertDate = null` |
| 6 | 存入 LocalStorage（`allProjects` 数组追加） |
| 7 | Toast 提示：`✅ 项目“{项目名}”已创建成功！` |
| 8 | **自动跳转至 `/active`（主看板）**，新项目卡片出现在列表最下方（折叠状态） |

### 7.6 取消按钮
- 点击 `取消` → 弹出确认框：“确定放弃创建吗？已填写的内容将丢失。”
- 确认后 → 跳转回 `/`（启动页）


## 8. 核心算法规范

### 8.1 排程算法（逆向 + 正向）

**输入**：ETD、ETA、7个节点的用户自定义耗时  
**输出**：每个节点的 `planStart` 和 `planEnd`

```typescript
// 伪代码
function generateSchedule(etd, eta, durations) {
  // nodeId: 1=出口报关, 2=集港捆扎, 3=海上运输, 4=到港换单, 5=进口清关, 6=海关查验, 7=内陆运输
  
  // === 国内段（倒推，以 ETD 为锚点）===
  const etdDate = dayjs(etd);
  const node1_end = etdDate;
  const node1_start = etdDate.subtract(durations[1], 'day');
  const node2_end = etdDate;
  const node2_start = etdDate.subtract(durations[2], 'day');
  
  // === 海上运输（正推，ETD → ETA）===
  const node3_start = etdDate;
  const node3_end = dayjs(eta);
  
  // === 境外段（正推，以 ETA 为锚点）===
  const etaDate = dayjs(eta);
  const node4_start = etaDate;
  const node4_end = etaDate.add(durations[4], 'day');
  const node5_start = etaDate;
  const node5_end = etaDate.add(durations[5], 'day');
  const node6_start = node5_end;
  const node6_end = node5_end.add(durations[6], 'day');
  const node7_start = node6_end;
  const node7_end = node6_end.add(durations[7], 'day');
  
  return { node1_start, node1_end, ..., node7_start, node7_end };
}
```

### 8.2 状态计算算法（每日自动）

```typescript
function calculateNodeStatus(node, today) {
  if (node.status === 'Done') return 'Done';
  
  if (today > node.planEnd) return 'Overdue';
  if (today >= node.planStart && today <= node.planEnd) return 'Active';
  return 'Pending';
}
```

### 8.3 连锁推迟算法

```typescript
function delayNodes(project, fromNodeId, delayDays) {
  const fromIndex = project.nodes.findIndex(n => n.nodeId === fromNodeId);
  for (let i = fromIndex; i < project.nodes.length; i++) {
    project.nodes[i].planStart = addDays(project.nodes[i].planStart, delayDays);
    project.nodes[i].planEnd = addDays(project.nodes[i].planEnd, delayDays);
    project.nodes[i].isDelayed = true;
    project.nodes[i].delayDays += delayDays;
  }
  // 同步更新 ETD / ETA
  if (fromNodeId <= 2) {
    project.etd = addDays(project.etd, delayDays);
    project.nodes.find(n => n.nodeId === 3).planStart = project.etd;
  }
  if (fromNodeId >= 4) {
    project.eta = addDays(project.eta, delayDays);
  }
  return project;
}
```

### 8.4 预警弹窗逻辑（每日首次打开触发）

```typescript
function checkAlerts(project, today) {
  if (project.status !== 'Active') return [];  // 已完成项目不触发
  if (project.lastAlertDate === today) return []; // 今日已提醒，去重
  
  const alerts = [];
  for (const node of project.nodes) {
    if (node.status === 'Done') continue;
    if (today > node.planEnd) {
      alerts.push({ priority: 'P0', node, days: diffDays(today, node.planEnd) });
    } else if (today === node.planEnd) {
      alerts.push({ priority: 'P1', node });
    } else if (today === node.planStart) {
      alerts.push({ priority: 'P2', node });
    }
  }
  return alerts;
}
```

**弹窗展示优先级**：P0（逾期）> P1（今日截止）> P2（今日启动），同级别多个节点合并展示。


## 9. 全局规则与边界处理

### 9.1 数据持久化

- 所有数据存储于浏览器 LocalStorage，键名：`logistics_projects`
- 任何修改操作（新建、标记完成、推迟、结束项目）后，立即写入 LocalStorage
- 页面加载时从 LocalStorage 读取数据，无数据时展示空状态

### 9.2 日期工具

- 统一使用 Day.js 进行日期计算
- 所有日期字符串格式：`YYYY-MM-DD`
- 比较日期时使用 `dayjs(date).startOf('day')` 确保时区一致

### 9.3 边界处理清单

| 场景 | 处理方式 |
| :--- | :--- |
| 主看板无进行中项目 | 显示空状态图 + “去启动页新建项目” 按钮（跳转 `/`） |
| 已完成列表为空 | 显示“暂无已完成项目” |
| 项目名称重名 | 允许重名，以 `projectId` 为唯一标识 |
| 海上运输耗时 < 1 天 | 表单校验报错：“ETA 必须晚于 ETD 至少 1 天” |
| 用户调整节点耗时导致冲突 | 保存时系统自动重算，若某个 `planStart` 早于今日，在日历网格中该节点标黄提醒 |
| 点击“结束项目”但节点未全部完成 | 弹窗额外确认：“尚有 X 个节点未标记完成，确定结束吗？” + 勾选框 `☑️ 我确认实际业务已提前结束` |
| LocalStorage 存储空间满 | 捕获 QuotaExceededError，提示用户清理旧项目 |
| 页面刷新 | 所有状态从 LocalStorage 重新读取，保持数据一致性 |

### 9.4 防错与确认弹窗

| 操作 | 确认弹窗 |
| :--- | :--- |
| 结束项目 | “确定结束吗？结束将移至归档。” |
| 取消新建 | “确定放弃吗？已填写内容将丢失。” |
| 标记节点完成 | 无弹窗，直接完成（轻量操作） |
| 推迟节点 | 模态框输入天数，确认后执行 |


## 10. UI视觉规范速查

### 10.1 三区颜色系统

| 区域 | 背景色（Hex） | 用途 |
| :--- | :--- | :--- |
| 境内段 | `#F0F7FF`（冰川蓝） | 区域背景、微型色块条左段 |
| 海运段 | `#D4E8FF`（深海蓝） | 区域背景、微型色块条中段 |
| 境外段 | `#FFF7E8`（沙土黄） | 区域背景、微型色块条右段 |

### 10.2 节点状态颜色

| 状态 | 指示灯颜色 | 色块颜色（半透明） | 文字颜色 |
| :--- | :--- | :--- | :--- |
| Overdue | `#F44336` 红 | `rgba(244,67,54,0.5)` | `#C62828` |
| Active | `#FFC107` 金 | `rgba(255,193,7,0.5)` + 外发光 | `#F57F17` |
| Done | `#4CAF50` 绿 | `rgba(76,175,80,0.4)` | `#2E7D32` |
| Pending | `#9E9E9E` 灰 | 不覆盖 | `#757575` |

### 10.3 卡片预警竖条

| 情况 | 竖条颜色 |
| :--- | :--- |
| 有逾期节点 | `#F44336`（红），宽度 5px |
| 有今日截止节点（无逾期） | `#FF9800`（橙），宽度 5px |
| 一切正常 | `#4CAF50`（绿），宽度 5px |

### 10.4 字体与间距

| 元素 | 字号 | 字重 | 行高 |
| :--- | :--- | :--- | :--- |
| 页面标题 | 20px | 600 | 28px |
| 卡片标题 | 16px | 600 | 24px |
| 正文/节点名 | 14px | 400 | 20px |
| 辅助文字/天数 | 12px | 400 | 18px |
| 卡片间距 | 16px | — | — |
| 单元格高度 | 48px | — | — |


## 11. 开发排期建议

| 阶段 | 模块 | 预估工时 | 优先级 |
| :--- | :--- | :--- | :--- |
| Phase 1 | 数据模型 + LocalStorage 工具函数 | 1天 | P0 |
| Phase 2 | 新建项目录入页（含排程算法） | 2天 | P0 |
| Phase 3 | 项目启动页（首页） | 1天 | P0 |
| Phase 4 | 主看板（折叠卡片 + 展开日历网格 + 海运压缩） | 3天 | P0 |
| Phase 5 | 节点操作（标记完成 + 连锁推迟） | 1.5天 | P1 |
| Phase 6 | 预警弹窗系统（每日首次触发） | 1天 | P1 |
| Phase 7 | 已完成项目列表 + 只读甘特图 | 2天 | P1 |
| Phase 8 | 结束项目 + 状态流转 | 0.5天 | P1 |
| Phase 9 | 联调测试 + 边界处理 + 空状态 | 1.5天 | P2 |
| **合计** | | **约 13.5 天** | |
