# 国际物流全流程跟踪系统 · 开发文档（PRD v6.2）

> 版本：v6.2 | 更新日期：2026-09-08 | 状态：可交付开发
> v6.1：节点模型改为「**国家模板驱动**」；巴西模板拆为 12 个流程节点（含角色泳道）；新建页新增国家选择（当前仅巴西）；缓冲并入预警；真实单证清单（含巴西进口证/四通道/编制部门与份数）。
> v6.2：新建页新增**出口港（起运港）选择**（现仅「青岛港 + 通用方案」，默认通用）；选择港口**仅影响国内段**——给相关节点/单证追加该港口的**线上办理平台备注**（如青岛港"云港通"、海事局一网通办），**不改动节点结构与耗时**，参考《国内报关》。

## 目录

1. [产品定位与合规映射](#1-产品定位与合规映射)
2. [技术栈](#2-技术栈)
3. [SQLite 数据模型（Schema）](#3-sqlite-数据模型schema)
4. [国家模板：节点 / 泳道 / 默认文件清单](#4-国家模板节点--泳道--默认文件清单)
5. [整体界面结构 & 路由](#5-整体界面结构--路由)
6. [页面一：启动页](#6-页面一启动页)
7. [页面二：主看板（甘特时间轴 + 文件清单）](#7-页面二主看板甘特时间轴--文件清单)
8. [页面三：新建项目录入页](#8-页面三新建项目录入页)
9. [页面四：已完成列表 & 只读甘特](#9-页面四已完成列表--只读甘特)
10. [核心算法规范](#10-核心算法规范)
11. [文件提醒引擎（含缓冲策略）](#11-文件提醒引擎含缓冲策略)
12. [托盘与提醒交互](#12-托盘与提醒交互)
13. [UI 视觉规范速查](#13-ui-视觉规范速查)
14. [工程化结构](#14-工程化结构)
15. [Mock 数据（用于验证算法）](#15-mock-数据用于验证算法)
16. [验收标准与用例清单](#16-验收标准与用例清单)
17. [非功能需求与边界用例](#17-非功能需求与边界用例)
18. [开发排期建议](#18-开发排期建议)

---

## 1. 产品定位与合规映射

### 1.1 产品定位

面向国际货运代理/外贸操作员的**项目全生命周期跟踪工具**，两条主线：

1. **流程时间轴（甘特）**：把项目拆成按国家模板定义的节点链，让新人看"现在做到哪一步、接下来发生什么"。
2. **文件清单提醒（核心）**：每个节点内置"要求准备/提交的官方单证清单"，按节点时间与单证状态自动提醒，**让新人照着做不遗漏单证、并能读懂流程**。

### 1.2 国家模板 + 港口子配置

- 系统按**目的国**管理流程模板（节点序列/泳道/默认耗时/默认单证/清关规则）。
- 每个国家模板可叠加**出口港（起运港）子配置**。港口子配置**仅修正国内段**：为相关节点/单证追加"线上办理平台"备注，**不增删国内节点结构、不改变耗时**。
- 现阶段模板：目的国仅 `巴西`；出口港可选 `青岛港` 或 `通用方案`（未指定，默认）。
- 新建项目页提供"国家"与"出口港"两个下拉，均为单选；港口默认"通用方案"。

### 1.3 与公司《项目管理办法》的映射

公司办法：五个阶段 × 十四个方面。本系统覆盖与"操作时间轴"重叠的部分：

| 公司方面 | 是否纳入 | 落点 |
| --- | :-: | --- |
| 操作前准备 | ✅ | 国内段单证提醒（报关/集港/捆扎/装船） |
| 实施操作 | ✅ | 12 流程节点（时间轴核心） |
| 保险 | ✅ | 海运段《保险申购单》提醒 |
| 进度管理 | ✅ | 《项目日报》《物流动态跟踪表》《项目进度报告》项目级常备提醒 |
| 文档管理 | ✅ | 节点内嵌单证清单 + 状态跟踪（巴西单证含编制部门与份数） |
| 立项/运营策划/编制手册 等 | ⚪ 预留 | 数据模型预留字段，本期不展开 |

### 1.4 核心设计原则

| 原则 | 说明 |
| :--- | :--- |
| 一页总览 | 启动页统管项目入口与今日待办 |
| 多项目并行 | 主看板同时跟踪多个进行中项目 |
| 国家模板 | 流程/单证/规则按目的国模板生成 |
| 角色泳道 | 每个节点标注负责角色，便于理解分工 |
| 单证内嵌 | 每节点自带默认单证清单，随项目创建自动生成 |
| 只读归档 | 已完成项目可查阅、不可编辑、不触发预警 |
| 海运压缩 | 海上运输段压缩，控制横向宽度 |
| 纯本地运行 | 数据存本地 SQLite，无后端、可离线 |

---

## 2. 技术栈

| 层级 | 选型 | 说明 |
| :--- | :--- | :--- |
| 语言 | Python | 3.10+ |
| GUI | PySide6（Qt6） | 多页面 `QStackedWidget` + 侧栏导航 |
| 数据库 | SQLite（`sqlite3`） | 单文件 `data/logistics.db` |
| 日期 | `datetime.date` | 统一 `YYYY-MM-DD` |
| UUID | `uuid4` | 项目 ID |
| 托盘 | `QSystemTrayIcon` | 常驻托盘 + 待办提醒 |
| 打包 | PyInstaller | 桌面分发可选 |

---

## 3. SQLite 数据模型（Schema）

### 3.1 projects（项目主表）

```sql
CREATE TABLE projects (
    project_id             TEXT PRIMARY KEY,       -- UUID
    project_name           TEXT NOT NULL,          -- ≤50字符
    country                TEXT NOT NULL,          -- 目的国模板：'BR'（现仅巴西）
    export_port            TEXT,                    -- 出口港(起运港)：'QD'=青岛港；NULL=通用方案
    status                 TEXT NOT NULL
        CHECK (status IN ('Active','Completed')),
    etd                    TEXT NOT NULL,          -- 预计离港日
    eta                    TEXT NOT NULL,          -- 预计到港日
    buffer_days            INTEGER NOT NULL DEFAULT 4, -- 机动缓冲天数（并入预警）
    actual_completion_date TEXT,
    create_date            TEXT NOT NULL,
    last_alert_date        TEXT,
    last_visited_page      TEXT,
    last_opened_project_id TEXT
);
```

### 3.2 nodes（节点，按国家模板生成，每项目 N 行）

```sql
CREATE TABLE nodes (
    project_id       TEXT NOT NULL,
    node_id          INTEGER NOT NULL,             -- 模板内序号 1..N
    node_name        TEXT NOT NULL,                -- 动词+名词
    role_label       TEXT NOT NULL,                -- 泳道角色（报关行/货代/船公司/境外代理/海关…）
    seq              INTEGER NOT NULL,
    area             TEXT NOT NULL
        CHECK (area IN ('DOME','SEA','OVERSEA')),   -- 境内/海运/境外
    default_duration INTEGER NOT NULL,              -- 模板默认耗时
    duration         INTEGER NOT NULL,              -- 当前生效耗时
    plan_start       TEXT NOT NULL,
    plan_end         TEXT NOT NULL,
    is_delayed       INTEGER NOT NULL DEFAULT 0,
    delay_days       INTEGER NOT NULL DEFAULT 0,
    status           TEXT NOT NULL DEFAULT 'Pending'
        CHECK (status IN ('Pending','Active','Overdue','Done')),
    actual_completion_date TEXT,
    remark           TEXT,                          -- 节点细化备注（吊机能力/航线/清关通道等）
    PRIMARY KEY (project_id, node_id)
);
```

### 3.3 files（单证清单，核心）

```sql
CREATE TABLE files (
    file_id            INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id         TEXT NOT NULL,
    node_id            INTEGER,                     -- 所属节点；NULL=项目级全程
    doc_name           TEXT NOT NULL,               -- 单证名称
    doc_type           TEXT NOT NULL
        CHECK (doc_type IN ('required','optional')),
    owner_dept         TEXT,                        -- 编制部门（巴西单证要求标注）
    copies             INTEGER,                     -- 份数（正本/复印件几份）
    due_node_id        INTEGER,                     -- 截止环节
    due_type           TEXT CHECK (due_type IN ('node_start','node_end',NULL)),
    remind_before_days INTEGER DEFAULT 3,           -- 提前提醒天数
    status             TEXT NOT NULL DEFAULT 'pending'
        CHECK (status IN ('pending','submitted')),
    due_date           TEXT,                        -- 建议提交/生成日期
    submitted_date     TEXT,
    owner              TEXT,                        -- 责任人
    note               TEXT,                        -- 单证细化备注
    is_default         INTEGER NOT NULL DEFAULT 1,
    FOREIGN KEY (project_id) REFERENCES projects(project_id)
);
CREATE INDEX idx_files_project ON files(project_id, node_id);
```

### 3.4 settings

```sql
CREATE TABLE settings (
    key   TEXT PRIMARY KEY,
    value TEXT
);
-- 键例：app_version, default_country, remind_before_default, theme
```

### 3.5 约定

- 日期一律 `YYYY-MM-DD`；写操作即时 `commit()`。
- 国家模板本身存于 `config.py`（单一来源，不在 DB 冗余）；落库时把模板节点/单证展开写入 nodes/files。
- 预留多国家：后续新增国家只改 `config.py` 增加模板即可，DB 与 UI 无需改结构。

---

## 4. 国家模板：节点 / 泳道 / 默认文件清单

### 4.1 巴西模板 · 12 流程节点

| nodeId | 节点名（动词+名词） | 默认耗时 | area | 泳道角色 | 细化备注 |
| :-: | :--- | :-: | :--- | :--- | :--- |
| 1 | 办理出口报关 | 3 | DOME | 报关行/货代 | 需登录中国电子口岸签订三方协议 |
| 2 | 安排港口操作/集港 | 1 | DOME | 货代/港口 | 集港通知、港杂费 |
| 3 | 执行捆扎固定 | 1 | DOME | 码头/理货 | 件杂货船自吊机，吊装能力≥100t，臂长≥36m |
| 4 | 实施货物装船 | 1 | DOME | 船公司/码头 | 装船单、大副收据 |
| 5 | 进行海上运输 | ETA−ETD（动态，默认45） | SEA | 船公司（中远海运） | 航线青岛→上海→香港→马六甲→Colombo→CapeTown→巴西Sepetiba，直航36-40天；正本提单 |
| 6 | 办理到港换单 | 1 | OVERSEA | 境外代理/船代 | 提前7个工作日核对正本提单副本 |
| 7 | 申请进口许可 LI | 1 | OVERSEA | 境外代理/进口商 | Siscomex 申请，须装运前完成否则罚款；有效期60天 |
| 8 | 进行进口清关申报 | 3 | OVERSEA | 境外代理/报关行 | 需3-7工作日；清关前付清进口税/ICMS 等 |
| 9 | 执行海关查验/放行 | 1 | OVERSEA | 海关/境外代理 | 绿/黄/红/灰四通道；不可预清关，须货到后清关 |
| 10 | 支付临时堆存费 | 1 | OVERSEA | 境外代理/码头 | 堆存费收据、放行通知单 |
| 11 | 安排境外内陆运输 | 2 | OVERSEA | 境外代理/运输公司 | 5轴半挂车，200hp以上 MACK/MAN 重型牵引车 |
| 12 | 完成工地交付 | 1 | OVERSEA | 收货人/项目组 | 签收单、随车单据 |

> 表中未给耗时者统一默认 1 天（到港、许可、查验、堆存、交付），新建页均可调。

### 4.2 巴西模板 · 默认单证清单（创建项目时写入 files 表）

**项目级（全程）常备：**

| doc_name | type | 编制部门 | 份数 | 时点 | 备注 |
| :--- | :--- | :--- | :-: | :--- | :--- |
| 《项目日报》 | required | 操作组 | — | 全程每日 | — |
| 《物流动态跟踪表》 | required | 操作组 | — | 全程每周 | — |
| 《项目进度报告》 | required | 项目部 | — | 全程每周 | — |
| 《批次实施计划》 | required | 项目部 | — | 节点1开始前 | 提前3天提醒 |
| 《人员安排计划》 | required | 项目部 | — | 节点1开始前 | 提前3天提醒 |

**按节点（巴西实操单证，含编制部门与份数）：**

| nodeId | doc_name | type | 编制部门 | 份数 | 时点 | 备注 |
| :-: | :--- | :--- | :--- | :-: | :--- | :--- |
| 1 | 出口报关单 | required | 报关行 | 1 | node_end | 中国电子口岸三方协议 |
| 1 | 商业发票 | required | 发货人 | 3正3复 | node_end | 需公证件，英/葡双语，注明HS/原产地/贸易术语；未呈报关单不符罚关税1-5% |
| 1 | 装箱单 | required | 发货人 | 3正3复 | node_end | 详细列明每箱货物；须与发票/提单信息一致（货单不符罚关税1-5%）；正本3份+复印件3份 |
| 1 | 出口合同 | required | 收货人 | 1正 | node_end | — |
| 1 | 出口许可证（如需） | required | 相关部门 | — | node_end | 涉证商品需备 |
| 2 | 集港通知 | required | 货代 | — | node_start | — |
| 2 | 港杂费单据 | required | 港口 | — | node_start | — |
| 3 | 绑扎加固方案 | required | 码头/理货 | — | node_start | 吊机≥100t/臂长≥36m |
| 3 | 装载计划 | required | 码头 | — | node_start | — |
| 4 | 装船单 | required | 码头 | — | node_end | — |
| 4 | 大副收据（理货单） | required | 大副/理货 | 1 | node_end | — |
| 5 | 正本海运提单 | required | 船公司 | 3正 | node_start | 需在 nodes表备注列标航线 |
| 5 | 随船箱单/发票 | required | 发货人 | 3正 | node_start | 与节点1装箱单为同一份；随船携带，到港后供巴西海关核查 |
| 5 | 《保险申购单》 | required | 商务部 | — | node_start | 发运前投保，CIF 条款 |
| 6 | 正本提单（换单） | required | 船代 | 1正 | node_start | 提前7个工作日核对副本 |
| 6 | 换单费收据 | required | 船代 | — | node_end | — |
| 7 | 非自动进口许可证申请书 | required | 境外代理/进口商 | — | node_start | Siscomex 申请 |
| 7 | 原产地证（如需） | optional | 发货人 | 1 | node_start | 优惠关税 FORM A/CO |
| 7 | ANVISA/INMETRO 认证 | required | 相关部门 | — | node_start | 巴西强制认证（涉及产品时） |
| 7 | CPF/CNPJ 税号 | required | 收货人 | — | node_start | 进口商税号 |
| 8 | 进口证 | required | 收货人 | 1正1复 | node_end | 外贸部签发；有效期60天 |
| 8 | 进口税费缴纳凭证 | required | 报关行 | — | node_end | II/IPI/PIS/COFINS/ICMS |
| 9 | 查验通知 | required | 海关 | — | node_start | 四通道 |
| 9 | 熏蒸证书（木包装） | required | 发货人 | 1正 | node_start | 木质包装必检；含非木质包装证明 |
| 9 | 危险品证明（如有） | optional | 发货人 | 1正 | node_start | — |
| 9 | 商品检验证明（二手/机器） | optional | 发货人 | — | node_start | 二手/机器装置需出具 |
| 10 | 堆存费收据 | required | 码头 | — | node_end | 超期仓储风险控制 |
| 10 | 放行通知单 | required | 海关/码头 | — | node_end | — |
| 11 | 送货通知 | required | 境外代理 | — | node_start | — |
| 11 | 提货单 D/O | required | 船代 | 1正 | node_start | 需核对清关单证一致 |
| 12 | 签收单 | required | 收货人 | — | node_end | — |
| 12 | 随车单据 | required | 运输公司 | — | node_end | — |

> 备注来源：《巴西清关》——商业发票需公证书/罚则、进口证编号须标注于所有文件、熏蒸证书、四通道时效（绿2/黄7-10/红15/灰20天-6月）等。这些"知识型备注"写入 `note`/`remark`，随节点与单证一起展示，帮助新人理解。

### 4.3 缓冲并入预警策略

- `projects.buffer_days` 默认 4 天，代表查验异常/拥堵/补文件的机动空间，**不作为时间轴节点**。
- 触发：当节点 9（查验）或 10（堆存）进入 Overdue 时，预警文案提示"**已消耗缓冲 X 天（预留机动 4 天）**"，X=实际逾期天数。

### 4.4 出口港配置（仅影响国内段的线上平台备注）

- 港口子配置不改节点结构/耗时，只在国内段相关节点的 `remark` 与单证 `note` 中追加"线上办理平台"提示。
- 落地：`config.py` 的 `PORTS` 字典，键为港口代码，值为 `{name, platform_notes:{node_id: 文案}}`。项目保存后，`file_checklist.bootstrap` 会把港口平台备注写入国内段节点/单证。

| 出口港 | 代码 | 对话框选项 | 说明 |
| :--- | :--- | :--- | :--- |
| 通用方案（未指定） | NULL | 默认 | 国内段使用通用平台：国际贸易单一窗口 / 中国电子口岸 / 互联网+海关，无港口专属流程 |
| 青岛港 | `QD` | 青岛港 | 追加青岛港专属平台备注（下表） |

**青岛港（`QD`）国内段平台备注（来源《国内报关》）：**

| 作用于国内节点 | 追加的平台提示 |
| :--- | :--- |
| 节点1 出口报关 | 报关单证经"国际贸易单一窗口→业务应用→口岸执法申报→货物申报"；企业备案经单一窗口或"互联网+海关"；签订电子口岸无纸化三方协议 [官网：singlewindow.cn / chinaport.gov.cn / online.customs.gov.cn] |
| 节点1 出口报关（查验场景） | 海关查验可经"青岛港云港通"平台在线预约查验 [qingdao-port.net] |
| 节点2 集港 | 港口操作经"青岛港云港通"办理装卸合同、流向填报、网上缴费 [qingdao-port.net] |
| 节点3 捆扎 | 涉及件杂货，吊装能力≥100t且臂长≥36m（同 §4.1 备注） |
| 节点4 装船 | 涉及危险品时：提前7天提交危险货物申报（危包证+MSDS）至就近海事局，经"海事一网通办"[zwfw.msa.gov.cn]；电子提单（如涉及）可由船公司经 GSBN 区块链平台签发 |

> 通用方案（NULL）：四个国内节点仅保留 §4.1 基础备注，不追加任何港口专属平台。

---

## 5. 整体界面结构 & 路由

单窗口 `QMainWindow`，页面以 Key 路由（见 §6-§9 表格），顶栏含"今日待办 ⛔N"徽章、右侧设置。

---

## 6. 页面一：启动页

布局同 v6.0：三入口卡片（进行中 / 已完成 / 新建）+ 顶部今日待办提醒条。功能要点：
- 统计 Active/Completed 数量、今日待办（节点截止/逾期 + required 单证 pending）。
- 点击提醒条 → 弹出"今日待办"汇总（见 §11.3）。
- 空态：无进行中项目显示引导。

---

## 7. 页面二：主看板（甘特时间轴 + 文件清单）

### 7.1 统计栏

`📊 进行中N · ⏰今日截止N · 🚨逾期N · 📄缺单证N`，任何项>0 对应文字标红。

### 7.2 折叠卡片（高 96px）

| 区域 | 占比 | 内容 |
| :--- | :--- | :--- |
| 左 | 22% | 状态标签（🟢/🟡/🔴）+ 项目名(加粗) + 灰ID + 国家徽标 🇧🇷巴西 |
| 中 | 48% | 微型三区色块条 + “当前:{节点名}” |
| 右 | 30% | “距ETD X天” + “进度 X/12 ✅” + “缺单证 N” + `⏬展开` |

### 7.3 展开卡片

**上区：甘特网格**
- 行：模板节点数 N（巴西=12）+ 1 行区域标识。
- 列：首节点 plan_start → 末节点 plan_end，每天一列；列头日期，周末灰。
- **泳道角色**（v6.1 新增）：每个节点行名称左侧显示小色块角色标签（报关行/货代/船公司/境外代理/海关/收货人等，颜色映射见 §13.5），悬停显示角色全称。
- 单元格分层：底层区域色 → 中层状态半透明色块 → 顶层文字（节点名缩写+剩余天数）。
- **出口港平台横幅（v6.2 新增）**：当项目 `export_port` 非空时，国内段区域标识行下方显示一条浅蓝提示条，如"🌐 出口港：青岛港 · 集港/查验/危险品申报见各节点线上平台备注（云港通 qingdao-port.net）"。选通用方案不显示。
- 海运压缩、节点操作列（状态灯/标记完成/推迟/📎）同 v6.0。

**下区：文件清单面板**
- 分组：项目级 → 各节点分组。
- 每行：`[✓/☐] 单证名(必) 编制部门×份数 状态 建议日 备注 ` + `提交`按钮。
- 状态色：待准备(灰)/临近(黄)/节点进行中仍缺(红)；已提交(绿，含提交日期)。
- 支持添加自定义单证、撤销提交。

---

## 8. 页面三：新建项目录入页

### 8.1 字段

| 字段 | 控件 | 必填 | 校验 |
| :--- | :--- | :--- | :--- |
| **目的国** | QComboBox | 是 | 现仅"巴西 🇧🇷"一项 |
| **出口港（起运港）** | QComboBox | 否 | 选项：`通用方案`（默认，v6.2 新增）/ `青岛港`；选通用 → `export_port=NULL`，选青岛港 → `'QD'` |
| 项目名称 | QLineEdit | 是 | 非空，≤50字符 |
| ETD | QDateEdit | 是 | ≥今日 |
| ETA | QDateEdit | 是 | >ETD（间隔≥1天） |

> "出口港"字段为空/选"通用方案"时写 `export_port=NULL`（使用通用平台备注，不改国内节点）；选"青岛港"写 `'QD'`。切换港口时，国内段节点/单证的线上平台备注即时联动更新预览。

### 8.2 高级排程参数（折叠面板，默认展开）

- 节点列表由所选国家模板生成（巴西=12 节点），每行：序号+名称+泳道角色+耗时输入(min=1,max=999)+`−`/`+`。
- 海上运输节点（5）默认=`ETA−ETD`；用户修改弹确认"将同步更新 ETA"→ 确认后 ETA=`ETD+新耗时`。
- 缓冲天数输入（巴西默认 4，见 §4.3）。
- 提示条："调整耗时后系统重算所有节点起止日期与单证建议日"。

### 8.3 保存流程

1. 校验（名称/ETD/ETA/耗时，同 v6.0）。
2. 成功：按模板 `generate_schedule` 生成 N 节点日期 → 写 projects（含 country + **export_port** + buffer_days）+ nodes（N 行，含 role_label/remark）→ `file_checklist.bootstrap(project, template, port)` 生成默认单证、算 due_date，并把所选港口的国内段平台备注写入对应 `remark`/`note` → commit。
3. Toast：`✅ 项目“{名}”已创建，生成 12 节点与 {单证数} 份单证清单。`（若选定港口，追加"国内段已套用{港口}线上平台备注"）
4. 自动切到主看板。

### 8.4 取消

确认"放弃创建？内容将丢失"→ 返回启动页。

---

## 9. 页面四：已完成列表 & 只读甘特

- 列表列：项目名 / 目的国 / ETD / ETA / 完成日期 / 总耗时；搜索模糊匹配；点击进详情。
- 只读甘特：黄色横幅；节点条形图（条形样式）+ 泳道角色标签；右侧`实际完成:日期`(绿) / `未执行`(灰)；**不显示红色逾期**；隐藏全部操作；单证只读。不触发预警（`status='Completed'` 跳过）。
- 结束项目：仅当全部节点 `status='Done'` 显示 `✅ 项目已完成，结束跟踪`；确认后 `status='Completed'`、`actual_completion_date=今日`；可提供"尚有 X 节点未完成，提前结束？☑️我确认已提前结束"。

---

## 10. 核心算法规范

### 10.1 排程算法（模板通用：境内串行 → 海运 → 境外串行）

```python
from datetime import date, timedelta

def generate_schedule(etd: date, eta: date, nodes_cfg: list) -> dict:
    # nodes_cfg: [{id, dur, area}, ...] 按 seq 升序
    if eta <= etd:
        raise ValueError("ETA 必须晚于 ETD 至少 1 天")

    plan = {}   # node_id -> (start, end)

    # 境内段：串行，最后一个 DOME 节点截止=ETD，向前倒推
    dome = [n for n in nodes_cfg if n['area'] == 'DOME']
    cursor = etd
    for n in reversed(dome):
        end = cursor
        start = cursor - timedelta(days=n['dur'])
        plan[n['id']] = (start, end)
        cursor = start

    # 海运段：ETD -> ETA
    sea = [n for n in nodes_cfg if n['area'] == 'SEA'][0]
    plan[sea['id']] = (etd, eta)

    # 境外段：串行，首节点起=ETA，向后正推
    oversea = [n for n in nodes_cfg if n['area'] == 'OVERSEA']
    cursor = eta
    for n in oversea:
        start = cursor
        end = cursor + timedelta(days=n['dur'])
        plan[n['id']] = (start, end)
        cursor = end

    return plan
```

> 海上运输动态耗时=`ETA−ETD`；校验时以实际值为准。境外内陆（节点11/12）并入 OVERSEA 段串行。

### 10.2 节点状态机

单一数据源：`nodes.status`（Done 持久化）+ `actual_completion_date`；实时展示用 `compute_node_status`，只读视图依据 Done/actual_completion。

```python
def compute_node_status(node, today):
    if node['status'] == 'Done': return 'Done'
    e, s = parse(node['plan_end']), parse(node['plan_start'])
    if today > e: return 'Overdue'
    if s <= today <= e: return 'Active'
    return 'Pending'
```

### 10.3 连锁推迟

同 v6.0 §10.3，推迟后立即触发全局刷新（新增逾期/缺单证即时反映）。

### 10.4 单证 due_date

```python
def compute_file_due(file_cfg, plan):
    if file_cfg.get('due_node_id') and file_cfg['due_type'] == 'node_end':
        return plan[file_cfg['due_node_id']][1]
    if file_cfg.get('due_node_id') and file_cfg['due_type'] == 'node_start':
        return plan[file_cfg['due_node_id']][0]
    return None   # 项目级周期型，无固定 due，靠周期提醒
```

---

## 11. 文件提醒引擎（含缓冲策略）

### 11.1 提醒分类与颜色

| 内部码 | 含义 | 触发条件 | 视觉 |
| :--- | :--- | :--- | :--- |
| NODE_OVERDUE | 节点逾期 | today>plan_end 且非 Done | 红 |
| NODE_END_TODAY | 今日截止 | today==plan_end 且非 Done | 橙 |
| NODE_START_TODAY | 今日启动 | today==plan_start 且非 Done | 黄 |
| BUFFER_USED | **消耗缓冲** | （若节点9/10 Overdue） | 红+提示文案 |
| FILE_MISSING_LATE | 单证临近 | today ≥ due−提前天数 且 pending | 黄 |
| FILE_MISSING_IN_ACTIVE | 节点进行中仍缺 | 所属节点 Active/Overdue 且 pending | 红 |
| FILE_LATE | 单证超建议日 | due_date 非空 且 today>due_date 且 pending | 红 |

### 11.2 每日重算

启动主界面/进主看板/托盘点击刷新时，对全部 Active 项目重算；推迟后立即全局刷新。

### 11.3 "今日待办"弹窗（去重）

- 主界面首次加载 `last_alert_date != today` 时弹出；弹完写 `last_alert_date=today`。
- 全量快照，按 P0（逾期/单证超期/消耗缓冲）→P1（今日截止/必备单证）→P2（今日启动）分组合并。
- 全量重算保证"同日先提醒后新增事件"不遗漏。

### 11.4 样例

```
📋 今日待办 · 2026-09-08
【需处理】
 · 项目A：节点⑨海关查验/放行 已逾期 3 天（已消耗缓冲 3 天，预留 4 天）
 · 项目A：必备单证《保险申购单》未提交（应于发运前）
 · 项目A：单证《熏蒸证书》超建议提交日 2 天
【进行中提醒】
 · 项目B：节点⑧进口清关申报 今日截止
[关闭] [进入主看板]
```

---

## 12. 托盘与提醒交互

托盘图标按待办颜色变化（灰=无/黄=有截止或单证临近/红=逾期或超期）；菜单：打开主界面/今日待办/退出；点图标恢复窗口；关闭窗口隐藏到托盘；退出需二次确认。

---

## 13. UI 视觉规范速查

### 13.1 三区颜色
境内 `#F0F7FF` / 海运 `#D4E8FF` / 境外 `#FFF7E8`。

### 13.2 节点状态颜色（同 v6.0）
Overdue `#F44336`；Active `#FFC107`+外发光；Done `#4CAF50`；Pending `#9E9E9E`（文字深色加粗保证可读）。

### 13.3 卡片预警竖条
逾期红/今日截止橙/正常绿（5px）。

### 13.4 字体与间距（同 v6.0）

### 13.5 泳道角色配色（新增）

| 角色 | 颜色 |
| :--- | :--- |
| 报关行/货代 | `#BBDEFB` 蓝 |
| 港口/码头/理货 | `#C8E6C9` 绿 |
| 船公司（中远海运） | `#B2EBF2` 青 |
| 海关 | `#FFE0B2` 橙 |
| 境外代理/船代 | `#E1BEE7` 紫 |
| 收货人/项目组 | `#F8BBD0` 粉 |

---

## 14. 工程化结构

```
international-logistics-tracker/
├── app.py
├── config.py                    # 国家模板 COUNTRIES + 出口港模板 PORTS（§1.2/§4.4）
├── db.py                        # sqlite 访问层
├── services/
│   ├── scheduler.py             # generate_schedule（§10.1）
│   ├── node_status.py           # compute_node_status（§10.2）
│   ├── delay.py                 # delay_nodes（§10.3）
│   ├── file_checklist.py        # 按模板 bootstrap 单证 + 算 due（§10.4）
│   └── reminder.py              # 提醒引擎：全量重算+缓冲策略+分组+去重（§11）
├── ui/
│   ├── main_window.py           # 主窗口 + QStackedWidget + 侧栏
│   ├── tray.py
│   ├── theme.py                 # 颜色/字体/角色配色（§13）
│   ├── dialogs.py
│   ├── pages/
│   │   ├── home_page.py
│   │   ├── dashboard_page.py
│   │   ├── new_project_page.py  # 含国家下拉 + 出口港下拉（青岛港/通用）
│   │   ├── completed_page.py
│   │   └── detail_page.py
│   └── widgets/
│       ├── gantt_grid.py        # 甘特（含海运压缩 + 泳道标签）
│       ├── file_panel.py
│       ├── mini_bar.py
│       └── node_row.py
├── data/logistics.db            # 运行时生成
├── mock_data.py
└── tests/
    ├── test_scheduler.py
    ├── test_status.py
    ├── test_delay.py
    ├── test_reminder.py
    └── test_schedule_expected.py  # §15 期望值回归
```

依赖：UI → services → db（+config）；UI 不直接写库。

---

## 15. Mock 数据（用于验证算法）

### 15.1 案例 A：青岛→巴西Sepetiba 光伏组件运输（巴西模板 + 青岛港，海运46天触发压缩）

- 项目名：`青岛→巴西Sepetiba 光伏组件运输`
- country='BR'，export_port='QD'（青岛港）
- ETD=`2026-09-10`，ETA=`2026-10-26`（海运 46 天）
- 耗时：`{1:3, 2:1, 3:1, 4:1, 5:46, 6:1, 7:1, 8:3, 9:1, 10:1, 11:2, 12:1}`
- 数据来源：用户提供的青岛→巴西甘特图（7 节点简化版 + 海运压缩），已映射到 12 节点模板

| nodeId | 节点 | plan_start | plan_end | 泳道角色 | 备注 |
| :-: | :--- | :--- | :--- | :--- | :--- |
| 1 | 出口报关 | 2026-09-04 | 2026-09-07 | 报关行/货代 | 青岛港·单一窗口/电子口岸 |
| 2 | 集港 | 2026-09-07 | 2026-09-08 | 货代/港口 | 青岛港·云港通 |
| 3 | 捆扎固定 | 2026-09-08 | 2026-09-09 | 码头/理货 | 吊机≥100t |
| 4 | 装船 | 2026-09-09 | 2026-09-10(ETD) | 船公司/码头 | — |
| 5 | 海上运输 | 2026-09-10 | 2026-10-26(ETA) | 船公司(中远海运) | 46天，触发压缩 |
| 6 | 到港换单 | 2026-10-26 | 2026-10-27 | 境外代理/船代 | 提前7工作日核对提单副本 |
| 7 | 进口许可LI | 2026-10-27 | 2026-10-28 | 境外代理/进口商 | Siscomex 申请 |
| 8 | 进口清关申报 | 2026-10-28 | 2026-10-31 | 境外代理/报关行 | 3天 |
| 9 | 海关查验/放行 | 2026-10-31 | 2026-11-01 | 海关/境外代理 | 四通道 |
| 10 | 临时堆存费 | 2026-11-01 | 2026-11-02 | 境外代理/码头 | — |
| 11 | 境外内陆运输 | 2026-11-02 | 2026-11-04 | 境外代理/运输公司 | 5轴半挂车 |
| 12 | 工地交付 | 2026-11-04 | 2026-11-05 | 收货人/项目组 | — |

> **甘特图视觉参考**：用户提供的参考图显示——黑色横向条、灰色虚线日列、列头显示天数（1-8, 50-55, 中间 `……` 省略压缩海运段）、节点名在左、条形在右。开发时照此风格实现 `gantt_grid` 控件。

### 15.2 案例 B：短程（不压缩）
ETD=`2026-05-01`，ETA=`2026-05-06`（间隔5天），耗时 `{1:2,2:1,3:1,4:1,5:5,6:1,7:1,8:1,9:1,10:1,11:1,12:1}`。
期望：节点1 04-28/04-29；2 04-29/04-30；3 04-30/05-01；4 05-01/05-01(ETD)；5 05-01/05-06；6 05-06/05-07；7 05-07/05-08；8 05-08/05-09；9 05-09/05-10；10 05-10/05-11；11 05-11/05-12；12 05-12/05-13。

---

## 16. 验收标准与用例清单

| # | 用例 | 期望 |
| :-: | :--- | :--- |
| U1 | 新建项目选"巴西"+出口港"青岛港"，ETD=2026-09-10/ETA=2026-10-26 | 生成 12 节点，日期匹配 §15.1，export_port='QD' |
| U2 | 新建后自动生成单证 | 按 §4.2 生成项目级+各节点全部单证，due_date 正确 |
| U3 | 新建页国家下拉 | 仅"巴西"一项；后端写入 country='BR' |
| U4 | **新建页出口港下拉** | 默认"通用方案"（export_port=NULL）；选"青岛港"→'QD' |
| U5 | **选出口港=青岛港** | 国内段节点1/2/4 的 remark、单证 note 追加云港通/海事局平台备注；节点结构仍为12、耗时不变 |
| U6 | **出口港=通用方案** | 四个国内节点仅基础备注，无港口专属平台文案；国内段不显示平台横幅 |
| U7 | ETD=ETA | 拒绝，提示"ETA 必须晚于 ETD 至少1天" |
| U8 | 名称为空 | 标红，不保存 |
| U9 | 修改海运(5)耗时为50 | 提示同步 ETA→确认后 ETA=ETD+50，节点5 及后续重算 |
| U10 | 标记节点⑨完成 | 状态→Done，写 actual_completion_date，不再逾期 |
| U11 | 推迟节点⑥ 3天 | 节点6-12 及 ETA +3天；境内（1-4）与海运起（ETD）不变 |
| U12 | 推迟节点② 2天 | 节点2-5 及 ETD +2天；境外段不变 |
| U13 | 全部 12 节点 Done | 显示"结束跟踪"，确认后转 Completed 入归档 |
| U14 | 只读详情 | 黄横幅、无红色逾期、无操作按钮、单证只读（含港口平台备注仅展示） |
| U15 | 节点⑨/⑩ Overdue | 今天待办出现"已消耗缓冲 X 天"提示 |
| U16 | 必备单证未提交且节点进行中 | 单证面板红/黄，托盘待办>0，今日待办含该项 |
| U17 | 提交一份单证 | 转已提交+写日期，待办数减少 |
| U18 | 同日二次启动 | 不重复弹"今日待办" |
| U19 | 启动后推迟产生新逾期 | 立即刷新即时反映 |
| U20 | 海运>25天 | 网格压缩≤7列，`…`Tooltip 正确 |
| U21 | 关闭主窗口 | 隐藏到托盘不退出；托盘退出才退出 |
| U22 | 归档搜索 + 进详情 | 模糊过滤，点行进只读 |
| U23 | 异常崩溃 | 每次 commit，重启可恢复不丢数据 |

---

## 17. 非功能需求与边界用例

### 17.1 非功能
| 项 | 要求 |
| :--- | :--- |
| 性能 | 初始化≤1s；100项目折叠渲染流畅；单项目甘特≤100ms |
| 安全 | 单连接串行写库、即时 commit |
| 备份 | 导出 JSON/CSV；直接拷贝 logistics.db |
| 编码 | 源码与读写统一 UTF-8 |
| 异常 | SQLite 满/只读 → 捕获 Toast，不闪退 |
| 可维护 | 分层 + 国家/港口模板集中在 config.py（COUNTRIES / PORTS） |

### 17.2 边界用例
| 场景 | 处理 |
| :--- | :--- |
| 主看板无进行中 | 空态引导去新建 |
| 某节点无单证 | 提示"该节点暂无单证，可添加自定义" |
| 节点 start 早于今日 | 网格该节点标黄"早于今日" |
| 多国扩展 | 仅改 config.py 增模板，DB/UI 不变 |
| 新增出口港（如上港/宁波） | 仅向 config.py 的 PORTS 增加条目即可，国内段平台备注沿用同一机制 |
| 已提交单证被推迟 | 已提交状态保留；due_date 重算显示最新建议日 |
| 跨明日 | 首次加载重算纳入新到期状态 |
| 数据文件损坏 | 校验表结构，损坏备份改名重建空库并提示 |

---

## 18. 开发排期建议

| 阶段 | 模块 | 预估 | 优先级 |
| :--- | :--- | :--- | :--- |
| Phase 1 | 工程骨架 + db + config（含巴西模板）+ 建表 | 2天 | P0 |
| Phase 2 | services（通用模板排程/状态/推迟/单证/提醒引擎）+ 单测 | 3.5天 | P0 |
| Phase 3 | 新建项目页（国家下拉 + 模板节点生成 + 单证 bootstrap） | 2.5天 | P0 |
| Phase 4 | 启动页 + 主看板（折叠卡片 + 统计栏） | 2天 | P0 |
| Phase 5 | 甘特网格（海运压缩 + 泳道角色标签）+ 节点操作 | 3.5天 | P0 |
| Phase 6 | 单证清单面板（提交/自定义/份数展示） | 2天 | P1 |
| Phase 7 | 今日待办 + 缓冲策略 + 托盘 | 2天 | P1 |
| Phase 8 | 已完成列表 + 只读甘特 + 结束项目 | 2天 | P1 |
| Phase 9 | 联调 + mock 回归 + 边界/打包 | 2天 | P2 |
| **合计** | | **约 21.5 天** | |

---

*本文档 v6.1 为可交付版；国家模板已抽象，后续扩展其他国家仅需扩展 config.py。*